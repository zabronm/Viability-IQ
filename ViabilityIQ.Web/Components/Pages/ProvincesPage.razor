@page "/settings/provinces"

@using ViabilityIQ.Web.Components.CommonComponents
@using ViabilityIQ.Shared.DataModels
@using ViabilityIQ.Application.Dtos
@using ViabilityIQ.Web.Components.Pages.PageFormComponents
@inherits Microsoft.AspNetCore.Components.ComponentBase

<div class="p-0 position-relative">
    <PageHeading _pageTitle="Provinces" CurrentPage="PageType.Province" _pageHeading="Provinces" />

    <main class="p-3 bg-white mt-2 mb-4" style="border-radius:8px;">
        <div class="app-workspace-shell">

            <ZabDataTableAdvanced TItem="ProvinceDto"
                                  Items="@provincesList"
                                  ItemName="Province"
                                  Columns="@tableColumns"
                                  IsLoading="@loadingStateActive"                                  
                                  ShowAdd="true"
                                  ShowDelete="true"
                                  OnDelete="DeleteSelectedProvince"
                                  GetRowId="@(x => x.ProvinceId)"
                                  OnAddRecordId="HandleFormExecution"
                                  OnEditRecordId="HandleFormExecution"
                                  OnPrintList="ExecutePrintFormatProcess"
                                  OnExportExcelList="ExecuteExcelExportProcess"
                                  OnEmailList="ExecuteEmailDistributionProcess" />
        </div>
    </main>

    @* ALIGNED: Merged duplicate canvas segments into a single entry block *@
    <ZabOffCanvas @ref="canvasShell" @bind-IsOpen="canvasOpenStatus" HeaderTitle="@formTitle" dWidth="500">
        @if (canvasOpenStatus)
        {
            <ProvinceFormComponent ProvinceId="@activeRecordId" OnSavedSuccess="@(res => ProcessExecutionFeedback(res))" />
        }
    </ZabOffCanvas>

    <footer class="py-4 bg-light mt-auto">
        <BFooter />
    </footer>
</div>