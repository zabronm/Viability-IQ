@* 
<button class="avatar-trigger-btn" @onclick="ToggleProfileMenu" @onfocusout="CloseProfileMenu">
    <div class="avatar-circle">ZM</div>
</button>
<div class="profile-card-dropdown @(isProfileOpen ? "show" : "")">
    <div class="profile-card-header">
        <div class="profile-avatar-large">ZM</div>
        <div class="profile-info">
            <h4 class="profile-name">Zabron Muyambo</h4>
            <span class="profile-role">Lead Developer</span>
        </div>
    </div>
    <div class="profile-card-body">
        <a href="profile" class="profile-dropdown-item">
            <i class="bi bi-person me-2"></i> My Profile
        </a>
        <a href="/register" class="profile-dropdown-item">
            <i class="bi bi-gear me-2"></i> Account Settings
        </a>
        <div class="dropdown-divider"></div>
        <a href="/logout" class="profile-dropdown-item text-danger">
            <i class="bi bi-box-arrow-right me-2"></i> Sign Out
        </a>
    </div>
</div> *@

@code {

}



<style type="text/css">

    .dropdown-card-menu, .profile-card-dropdown {
        position: absolute;
        top: 45px;
        left: 0;
        width: 150px !important;
        background: #ffffff;
        border: 1px solid #e2e8f0;
        border-radius: 12px;
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        display: none;
        z-index: 500;
    }

    .profile-card-dropdown {
        left: auto;
        right: 0;
        width: 260px;
    }

        .dropdown-card-menu.show, .profile-card-dropdown.show {
            display: block;
        }

    .dropdown-card-header, .profile-card-header {
        padding: 12px 16px;
        background-color: #f8fafc;
        border-bottom: 1px solid #e2e8f0;
        font-size: 0.75rem;
        font-weight: 700;
        text-transform: uppercase;
        color: #64748b;
    }

    .profile-card-header {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 16px;
    }

    .profile-avatar-large {
        width: 44px;
        height: 44px;
        border-radius: 50%;
        background-color: #e0e7ff;
        color: #000099;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
    }

    .profile-info {
        display: flex;
        flex-direction: column;
    }

    .profile-name {
        margin: 0;
        font-size: 0.9rem;
        font-weight: 700;
        color: #1e293b;
    }

    .profile-role {
        font-size: 0.72rem;
        color: #64748b;
    }

    .profile-card-body {
        padding: 8px 0;
    }

    .profile-section-title {
        font-size: 0.68rem;
        font-weight: 700;
        text-transform: uppercase;
        color: #94a3b8;
        padding: 8px 16px 4px 16px;
    }

    .profile-menu-item {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 8px 16px;
        color: #475569;
        text-decoration: none;
        font-size: 0.85rem;
    }

        .profile-menu-item:hover {
            background-color: #f8fafc;
            color: #1e293b;
        }

    .dropdown-divider {
        height: 1px;
        background-color: #e2e8f0;
        margin: 6px 0;
    }

    .logout-item {
        color: #ef4444;
    }

        .logout-item:hover {
            background-color: #fef2f2;
            color: #ef4444;
        }
</style>